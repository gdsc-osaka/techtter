import PostForm from '@/components/postForm';

interface Props {
    params: {
        topicId: string[];
    };
}

export default function PostListPage(props: Props) {
    return (
        <div>
            PostListPage
        </div>
    );
}
